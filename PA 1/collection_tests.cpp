#include <stdexcept>
#include "collection.h"
#include "iostream"

using std::cout;

int main() {
    // Collection<int> list;
    // list.insert(0);
    // list.insert(1);
    // list.remove(0);
    // list.remove(0);
    // list.insert(2);
    // list.insert(3);
    // list.remove(4);
    // list.is_empty();

    return 0;
}